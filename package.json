{
  "name": "my-expenses",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd api && npm install) || (cd my-expenses-front-end && npm install)",
    "server": "nodemon ./api/index.js",
    "client": "cd my-expenses-front-end && npm start"
  },
  "dependencies": {
    "if-env": "^1.0.4",
    "nodemon": "^2.0.22"
  }
}
